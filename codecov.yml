coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch:
      default:
        target: 80%
  
  ignore:
    - "src/**/*.test.ts"
    - "src/**/*.d.ts"
    - "dist/**/*"
    - "coverage/**/*"
    - "node_modules/**/*"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
